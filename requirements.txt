# ── PyTorch: CPU-only build (Streamlit Cloud has no GPU) ─────────────────────
# Pulls torch from the CPU wheel index so sentence-transformers does NOT drag in
# ~2.5 GB of unused NVIDIA CUDA libraries (cublas / cudnn / nccl / triton / …).
# The `+cpu` build has no nvidia-* deps — smaller, faster builds, easier on the
# free tier's disk/RAM. (No GPU on Streamlit Cloud, so nothing is lost.)
--extra-index-url https://download.pytorch.org/whl/cpu

# ── Web UI ───────────────────────────────────────────────────────────────────
streamlit>=1.55.0

# ── LangChain stack (retrieval only) ─────────────────────────────────────────
# Used ONLY for retrieval now: langchain-community (FAISS) + langchain-huggingface
# (local embeddings). No OpenAI/GPT anywhere — the LLM and vision are both Claude.
langchain>=1.3.1
langchain-core>=1.4.0
langchain-community>=0.4.1
langchain-text-splitters>=1.1.2
langchain-huggingface>=1.2.2

# ── Anthropic SDK (ALL LLM work: Claude Sonnet 4.6 + Haiku 4.5) ───────────────
# Runtime generation/Q&A/gap, the AI router, web search, AND build-time vision.
# Needs output_config.effort + web_search tool support; 0.112.0 is verified-working.
anthropic>=0.112.0

# ── Embeddings / retrieval ───────────────────────────────────────────────────
torch==2.12.1+cpu          # CPU build (see --extra-index-url at top) — no CUDA libs
sentence-transformers>=5.5.0
faiss-cpu>=1.13.2
rank-bm25>=0.2.2
tiktoken>=0.7.0

# ── HTTP (used by various deps) ──────────────────────────────────────────────
requests>=2.31.0

# ── Document parsing (DOCX / PDF / PPTX + OCR) ───────────────────────────────
pypdf>=4.0.0
pdfplumber>=0.10.0
PyMuPDF>=1.23.0
pdf2image>=1.16.0
pytesseract>=0.3.10
Pillow>=10.0.0
python-docx>=1.1.0
python-pptx>=0.6.23
unstructured>=0.12.0
lxml>=5.0.0
llmwhisperer-client>=2.0.0  # optional: layout-preserving PDF parsing (set WHISPER_API_KEY)

# ── Cloud storage (optional Supabase tab) ────────────────────────────────────
supabase>=2.30.0

# ── Flowchart rendering (procedure workflow diagrams) ────────────────────────
# Python binding only; the `dot` system binary is provided via packages.txt on
# Streamlit Cloud (apt: graphviz) and must be installed locally for Word export.
graphviz>=0.20.1

# ── Misc ─────────────────────────────────────────────────────────────────────
pydantic>=2.0.0
python-dotenv>=1.0.0
tqdm>=4.66.0
